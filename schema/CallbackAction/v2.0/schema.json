{
  "$id": "https://schema.nfh.global/CallbackAction/v2.0",
  "x-iri": "https://schema.nfh.global/CallbackAction/v2.0",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "CallbackAction",
  "x-tags": [
    "common"
  ],
  "deprecated": true,
  "type": "object",
  "description": "DEPRECATED. This schema is structurally invalid and does not validate any payloads — the oneOf keyword was incorrectly nested inside properties, which is not valid JSON Schema. The $id also lacked a version segment. Callback actions are those with on_ prefixed endpoints (e.g. beckn/on_discover, beckn/on_confirm). This schema will be removed in a future major version."
}
